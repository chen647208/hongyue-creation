# 48 后续任务总表（排序）

## 背景与目标

34–47 与 26/28 的设计稿铺开了大量能力，缺一份把它们按依赖与收益排好序的总表。本表是执行口径：编号、任务、依据、依赖、验收、规模；落地后从本表移除，回写对应 `docs/features/`。

## 排序原则

1. 先做"零新依赖、复用既有底座、多域受益"的项。
2. 被多项依赖的地基先做（查询/块引用 → 跨域视图；修订/快照 → 时间线版本）。
3. 每个创作域的 A 档补齐优先于单域 S 档打磨。
4. 每项独立可测、可提交；出现第五类落点（核心/视图类型/资源型插件/逻辑型插件之外）即回评审。

## 阶段 0 收口与地基

本阶段已收口：工具参数 JSON Schema 校验（0.4）、发行档 webnovel/literary 差异化（0.5）、MCP 检索走 FTS + 标题回退（0.6）、插件逻辑贡献权限门与清单校验（0.3）、覆盖率门线阶梯上调（0.7）。`contributes.renderers` 因协议缺失挂起（见文末）。0.1/0.2 判定不做（见文末）。

## 阶段 1 查询、块引用与素材隔离（45，推荐先做）

已落地：视图即保存的查询（条件与或非/嵌套、计算列、聚合、多套具名视图、纯函数投影重算）；块级稳定标识（编辑器内生成/保留/重排不变/去重）+ 块 ID 落盘（DSL 块锚 `^id` 往返，编译/统计前剥离）；素材隔离（`Chapter.material` 单源，字数/编译/检索三处口径，检索素材优先）；块引用与嵌入（`((^id))`/`!((^id))`，反向引用面板、失链提示与跳修、环检测，导出统一展开）。

## 阶段 2 修订、批注与关联（38）

已落地：修订标记（快照/修订版为基线的字符级差异，逐处接受/拒绝并写回正文，字数与导出随正文同源更新）；行内批注（选区挂批注、回复、解决/重开，块标识 + 偏移 + 引用锚定，失锚提示，侧车存储不进入正文与导出）；角色登场章节列表与跳转；正文→章纲提取（预览勾选、只填空不覆盖）。

## 阶段 3 同步传输与冲突（36）

已落地：同步集合补齐——边参与插入与合并（稳定键 `from/to/kind/role` 去重、重复导入幂等、同 id 冲突转人工）；传输接口与三后端（本地目录 / WebDAV / S3 兼容 SigV4，主进程实现 + IPC，凭据入钥匙串，各带连通测试与有限重试）；冲突三选（保留副本 / 应用远端 / 标记待处理）+ 自动恢复记录；退出导出到配置后端，失败下次启动提醒并可重试。

## 阶段 4 AI 上下文注入与可信检索（37）

已落地：上下文注入（按章节/实体/关键词推断目标，装配正文片段、细纲、前情、实体、知识库、时间线、检索命中；逐条来源标注与触发原因；开关与单条关闭；预算 6000 单源常量、超预算按优先级裁剪并记录被裁条目；带 `original` 的引用逐字校验，不一致剔除）；可信检索（命中规范化为 `Citation`，去重排序，空结果明确措辞不编造；`core.text.search`/`semanticSearch` 返回 `found`/`citations`；答复按出处分栏并可点跳来源）。

## 阶段 5 编译与导出（39）

已落地：编译档案（可命名保存/复用/删除，YAML 导入导出；范围 `from/to`、素材口径、标题层级、分卷/前言/后记、目录开关与深度；默认档案逐字等价现状）；管线支持分卷/目录/前后置页块，md/txt/html/rtf 四渲染器按 kind+level 序列化，PDF/ePub/DOCX 既有入口接线；块锚与批注/修订不入正文，引用统一展开，字数与导出同源。

未做：ODT 渲染器、分镜表与口播时长、分卷/前言/后记的导出对话框专用选择器（仅档案层支持）。

## 阶段 6 时间线深化与版本（46）

| 编号 | 任务 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|
| 6.1 | 非破坏操作：插入/覆盖/滑动（可逆、可回溯） | 2.1 | 撤销后完全还原 | 大 |
| 6.2 | 草稿矩阵 ↔ 成稿轨道同源投影 | 6.1 | 改一处另一处一致 | 中 |
| 6.3 | 快照比较与逐段/整体回滚，回滚前自动再快照 | 2.1 | 回滚可再撤销 | 中 |
| 6.4 | AI 试错快照：事务前自动、会话内可回滚，与正式历史分离 | 6.3 | 正式历史不受影响 | 中 |

## 阶段 7 跨设备与移动端（35）

| 编号 | 任务 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|
| 7.1 | 响应式 PWA 落地：书库/阅读/轻编辑/统计/助手 | 3.2 | 手机宽度无塌缩无强制横滚 | 大 |
| 7.2 | 移动端编辑落盘与冲突副本 | 7.1、3.1 | 改一章落盘、重启仍在 | 中 |
| 7.3 | 触控与无障碍：命中区、删除二次确认、系统字号与减少动效 | 7.1 | 键鼠与触控等价 | 中 |

## 阶段 8 跨域视图与脚本层（47）

| 编号 | 任务 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|
| 8.1 | 图视图：DSL 解析→布局→渲染，拖拽回写 | 1.1 | 重开一致 | 大 |
| 8.2 | 画布视图：坐标节点+连线，开放格式序列化与导入导出 | 1.1 | 外部工具可互通 | 大 |
| 8.3 | 图表视图：字段→通道声明，按需加载 | 1.1 | 轴/图例由声明派生 | 中 |
| 8.4 | 脚本层：命令管道+变量+条件循环+命名子程序+事件触发 | 0.3 | 停用后手动完整 | 大 |
| 8.5 | 协作收敛不变量：版本号+随机决胜+墓碑+分数排序 | 8.2 | 有单测 | 中 |

## 阶段 9 创作域 A 档补齐

| 编号 | 任务 | 依据 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|---|
| 9.1 | 非虚构：引文/脚注/参考文献、交叉引用、对照视图 | 41 | 5.1 | 一份非虚构示例导出带文献表与脚注 | 大 |
| 9.2 | 分支叙事：场景/选择项/变量、可达性校验、跳转表 | 42 | 1.1 | 孤儿与未定义变量可报可定位 | 大 |
| 9.3 | 绘本：页结构、图位与替换、图文页导出 | 42 | 5.1 | 缺图显示占位不崩版 | 中 |
| 9.4 | 诗歌：分行/分节结构与留白导出 | 34 | 5.1 | 分行不被段落合并 | 小 |

## 阶段 10 生态与运行时（40）

| 编号 | 任务 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|
| 10.1 | 插件目录与一键安装/更新/卸载，签名与来源校验 | 0.3 | 篡改签名被拒、卸载清理干净 | 大 |
| 10.2 | 多助手会话：会话隔离、归档、并行任务 | 无 | 上下文互不污染 | 大 |
| 10.3 | MCP 资源面补全并自举（内置助手走同一面与待审箱） | 0.6 | 外部与内置同权同源 | 中 |
| 10.4 | 联网搜索/翻译作为插件（受控网络门 + 不可信输入） | 4.4 | 核心不内置、可关闭 | 中 |
| 10.5 | 本地推理接入层（进程管理/模型列举/本地端点） | 无 | 关闭即回落远程网关 | 中 |

## 阶段 11 无障碍与国际化品质（43，可并行）

| 编号 | 任务 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|
| 11.1 | axe 审计扩到全部主要面板，欠账棘轮只减不增 | 各面板 | 出现违规即失败 | 中 |
| 11.2 | 对比度达 AA，边界场景取安全值 | 11.1 | Linux/Windows 均通过 | 中 |
| 11.3 | 中英键集对齐校验与语言切换断言 | 11.1 | 零缺失零多余 | 中 |
| 11.4 | 键盘路径全覆盖：建书→工作台→分区→设置→弹层 | 11.1 | 全链路可键盘完成 | 中 |

## 有意挂起（不做，非欠账）

- 卡片服务从 `shared/services/cards` 迁入 `features/cards`：会重新引入 `assistant→cards`、`settings→cards` 跨 feature 依赖债（与 02 的边界规则和既有清理方向相反）；卡片服务属跨功能共享，保留在 `shared/services/cards`，`features/cards` 目录仅在未来出现卡片专属 UI 时使用。
- `foreshadowing`/`consistency` 服务从 `shared/services` 迁入功能域：同卡片，属跨功能共享逻辑，迁入会引入跨 feature 依赖债；保留在 `shared/services`，域目录只承载 UI。
- `contributes.renderers` 接线：现有渲染器契约为同步 `render()`，而插件可执行逻辑只能经异步沙箱；在补出 `renderers.json` 描述符协议（函数引用 + 同步渲染方案）前不改动。
- 正文以文件为源、追加式事件日志（用户已明确暂缓，见 28）。
- 协作 B/C 档的评论权限与工作流（协作只留缝，见 29）。
- 平台化：发布、社区、收益、全勤、平台合规预审。
- CAD/三维参数化、游戏运行时脚本、闭源云工具格式与依赖。
- Tauri/移动原生栈：移动需求出现前不迁。

## 执行约定

- 每项落地走同一门禁：`npm run verify`，运行时/UI 改动跑 `npm run test:e2e`，独立提交。
- 功能落地同步 `docs/features/` 对应篇，本表移除该项。
- 许可审查：只借鉴设计，不引入 copyleft 代码；重型运行时仅限可选插件。
- 体积与性能：渲染器按需加载，纳入体积预算。
