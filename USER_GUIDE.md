# 红月创作使用教程

## 从零开始创作你的小说

### 目录
1. [软件介绍与安装](#软件介绍与安装)
   - [软件概述](#软件概述)
   - [安装与配置](#安装与配置)
2. [核心功能使用指南](#核心功能使用指南)
   - [项目管理](#项目管理)
   - [创作流程详解](#创作流程详解)
3. [高级功能使用](#高级功能使用)
4. [实用技巧与最佳实践](#实用技巧与最佳实践)
5. [故障排除](#故障排除)
6. [附录](#附录)

---

## 软件介绍与安装

### 软件概述

**红月创作** 是一款专为小说创作者设计的桌面应用程序，它支持 DeepSeek、Kimi、GLM、通义千问、MiniMax、Gemini、Claude、GPT 与 Ollama 本地模型等多种渠道，提供从灵感生成到完整小说创作的全流程辅助工具。

#### 主要功能特点：
- **AI 辅助创作**：从灵感、角色、大纲到章节的完整创作流程
- **本地数据存储**：所有项目数据保存在本地，保护隐私
- **知识库管理**：上传参考资料并在创作中引用
- **多模型支持**：可配置不同的 AI 模型参数
- **流式输出**：实时查看 AI 生成内容
- **历史记录**：完整记录所有 AI 生成内容

#### 适用人群：
- 小说作家和写作爱好者
- 需要 AI 辅助的创作者
- 希望提高写作效率的作者
- 学习小说创作的新手

### 安装与配置

#### 系统要求
- **操作系统**：Windows 10/11（也支持 macOS 和 Linux）
- **内存**：建议 8GB 以上
- **存储空间**：至少 500MB 可用空间
- 说明：发布版是开箱即用的桌面程序，不需要自行安装 Node.js（Node 只在从源码开发时需要）。

#### 安装步骤

1. 从发布页面下载对应系统的安装包
2. 双击运行 `红月创作.exe`（Windows）完成安装并启动

#### 首次启动设置

1. **AI 模型配置**（用 AI 功能前需要）：
   - 点击顶栏右侧的模型名称（如"未选模型"）
   - 进入"模型设置"，选官方渠道填 Key 即可（DeepSeek/Kimi 等）
    - 不配模型也能纯手写，AI 按钮提示未配置原因

2. **创建第一本书**：
    - 首次启动会弹出向导，点"跳过"即建一本空白书（留在书籍库，点书卡进入工作区；选人设会顺带打开模型设置）
    - 之后在书籍库点击"新建书籍"（直接建空白书进工作区）
   - 或"从模板新建"（空白/复制/示例）
   - 在灵感分区输入初始想法，点击"生成书名与简介"
   - 开始你的创作之旅

---

## 核心功能使用指南

### 项目管理

#### 创建新书
1. 在书籍库点击"新建书籍"，自动进入工作台灵感分区
2. 在文本框中输入你的初始灵感
3. 点击"生成书名与简介"
4. 系统会以该书名建书并命名

#### 保存和加载项目
- **自动保存**：所有更改都会自动保存到本地
- **手动保存**：无需手动操作，系统自动处理
- **加载项目**：重新启动应用后，上次的项目会自动加载
- **标签分组**：卡片菜单"打标"给书贴标签（逗号或空格分隔），点顶部标签筛书

#### 项目导入/导出
1. **导出项目**：
   - 在书籍库用卡片菜单导出单本书，或用顶栏同步导出同步包
   - 选择保存位置
   - 项目数据将导出为 JSON 文件

2. **导入项目**：
   - 在书籍库点击"导入"
   - 选择之前导出的 JSON 文件
   - 确认导入，项目数据将恢复

#### 项目备份与恢复
- 数据存储位置：Windows `%APPDATA%/hongyue-creation/`、macOS `~/Library/Application Support/hongyue-creation/`、Linux `~/.config/hongyue-creation/`
- 老版本升级：首次启动自动把旧目录（`novalocal-ai-novelist`）搬到新位置，旧目录改名 `.legacy` 保留，双重保险
- 定期备份建议：使用导出功能备份重要项目
- 恢复出厂设置：在设置中可进行数据重置

### 创作流程详解

红月创作采用五个分区，引导你完成完整的小说创作（左侧图标栏自由切换，工作台内可用 `Ctrl+1..5` 快捷跳转，右上角有下一步建议）：

#### 分区一：灵感生成

**功能说明**：将零散的想法转化为完整的小说创意

**操作步骤**：
1. **输入灵感碎片**：
   - 在文本框中输入任何想法：题材、关键词、背景设定等
   - 示例："一个在魔法学院学习的普通学生，突然发现自己有特殊能力"

2. **引用知识库**（可选）：
   - 点击"上传新资料"上传参考文档
   - 选择要引用的知识条目
   - 支持的文件格式：.txt, .md, .json, .csv

3. **选择输出模式**：
   - **流式输出**：实时查看生成过程（推荐）
   - **传统输出**：一次性显示完整结果

4. **生成内容**：
   - 点击"生成书名与简介"
   - 等待 AI 生成结果
   - 可随时暂停、继续或停止生成

5. **编辑结果**：
   - 修改生成的书名
   - 编辑 AI 生成的简介
   - 保存到项目

**实用技巧**：
- 灵感越具体，生成结果越精准
- 上传相关参考资料可提高生成质量
- 使用流式输出可实时调整生成方向

#### 分区二：世界构建中心

**功能说明**：管理创作所需的所有参考资料（知识库、地点、势力、时间线、规则）

**操作步骤**：
1. **上传资料**：
   - 点击上传区选择文本文件（.txt/.md/.json/.csv）
   - 系统自动提取内容并分类

2. **组织知识**：
   - 查看所有上传的资料
   - 按类别筛选
   - 编辑知识条目名称和内容

3. **引用知识**：
   - 在后续步骤中可选择引用特定知识
   - AI 会基于引用的资料进行生成

**实用技巧**：
- 上传角色设定、世界观文档等参考资料
- 为知识条目添加清晰的名称便于查找
- 定期整理知识库，删除不需要的资料

#### 分区三：角色与势力

**功能说明**：创建小说中的角色体系

**操作步骤**：
1. **创建角色**：
   - 点击"添加角色"按钮
   - 填写角色基本信息：姓名、年龄、性别等
   - 添加角色背景和性格特点

2. **AI 生成角色**：
    - 使用 AI 自动生成角色背景故事
    - 基于灵感生成相关角色

3. **角色关系**：
   - 设定角色之间的关系
   - 可视化角色关系图
   - 调整角色重要性

**实用技巧**：
- 为主角创建详细档案
- 为配角设定关键特征即可
- 使用关系图理清角色网络

#### 分区四：结构（大纲/细纲，一页两段）

**功能说明**：创建小说的整体结构：先在大纲子页定主线，再在细纲子页拆章节

**操作步骤**：
1. **生成大纲**：
   - 基于灵感和角色生成小说大纲
   - 选择大纲模板或自定义
   - 调整故事结构和节奏

2. **编辑大纲**：
   - 自由编辑 AI 生成的大纲
   - 设定关键情节点

3. **生成章节细纲**：
   - 切到细纲子页，基于大纲自动生成章节列表
   - 为每个章节生成概要，可手动新建、拖拽排序
   - 全量生成会保留已有正文，只更新标题与细纲

4. **进入写作**：
    - 点击章节的"撰写本章"弹出生成窗口：点"确认生成正文"按细纲写本章，或切批量模式一次生成 5/10 章；点"仅进入编辑器"则不生成、直接手写
    - 开始撰写正文内容

**实用技巧**：
- 先生成整体框架再细化
- 为重要章节添加详细要点
- 保持章节长度相对均衡，考虑读者的阅读节奏

#### 分区五：写作编辑器

**功能说明**：实际的写作编辑环境

**操作步骤**：
1. **写作界面**：
   - 全屏写作编辑器
   - 左侧章节导航
   - 右侧写作区域

2. **AI 辅助写作**：
    - 选中文字用悬浮菜单做润色/扩写
    - 用生成弹窗按细纲写正文、续写段落（可单章或批量 5/10 章）
    - 左侧边栏可改细纲、提炼本章摘要、跳转章节
    - 点工具栏伏笔按钮追踪埋设与回收，AI 可扫描本章自动标记回收
    - 一致性检查在世界分区；右侧 AI 助手用 `Ctrl+J` 开关

3. **内容管理**：
   - 自动保存所有更改
   - 查看章节历史版本
   - 导出章节内容
   - 拆分/合并章节：工具条剪刀按钮从光标处拆成新章；合并按钮把下一章并入本章
   - 查找替换：`Ctrl+F` 打开章内查找条
   - 跨书全文检索：书籍库顶部「全文检索」，搜全部书的正文与知识库，点击直达

**实用技巧**：
- 先完成初稿再优化
- 合理使用 AI 辅助，保持个人风格
- 定期保存和备份

---

## 高级功能使用

### 全局助手

**功能**：随时获取 AI 写作建议

**使用方法**：
1. 点顶栏右侧 AI 图标或按 `Ctrl+J` 展开右侧边栏
2. 输入你的问题或需求（可用 `/角色` 等快捷命令建卡）
   - 支持追问上文（近期对话自动携带）；不满意可点"重新生成"按上轮重跑
   - 可发图片（png/jpg/webp，5MB 内）；开计划模式只出计划不执行
3. 写操作会进待审箱，批准后才落库
4. 可将建议复制到编辑器中
5. 未配置可用模型时发送和分析按钮会禁用并提示，先去模型设置里配置
6. 内置技能会在合适时机自动触发；点历史/会话事件流可查看每轮 Token 用量汇总

**适用场景**：
- 写作卡顿时获取灵感
- 需要特定描写建议
- 角色对话优化
- 情节发展建议

### 模型设置

**功能**：自定义 AI 模型行为

**配置选项**：
1. **渠道密钥管理**：
   - 添加官方渠道填 Key，或配 OpenAI 兼容网关/Ollama 本地模型
   - 测试连接状态

2. **模型参数**：
    - **温度**：控制创造性（0-2.0，默认 0.7）
    - **最大令牌数**：控制生成长度

3. **提示词模板**：
    - 查看和编辑各步骤的提示词
    - 创建自定义模板

### 历史记录

**功能**：查看所有 AI 生成内容

**访问方式**：
1. 有 AI 生成历史后，顶栏会出现"历史记录"按钮（无历史时不显示）
2. 查看按时间排序的生成记录，或切到会话事件流看 Agent 执行过程
3. 筛选特定步骤或章节的历史
4. 重新使用历史生成内容

**实用价值**：
- 追溯创作思路变化
- 复用优秀的生成内容
- 分析 AI 生成模式

### 伏笔追踪

**功能**：记录每条伏笔的埋设章节，逾期未回收时标红提醒

**使用方法**：
1. 在写作编辑器点工具栏伏笔按钮打开面板
2. 填写简述与内容添加伏笔（会自动关联当前章节）
3. 点"AI 检测本章回收"让 AI 扫描正文并自动标记回收
4. 检测需要先配置可用模型；手动标记、废弃、删除随时可用

### 版本检查

**功能**：检查软件更新

**使用方法**：
1. 点击顶部版本号旁边的刷新按钮
2. 系统自动检查更新
3. 如有新版本，显示更新信息，点按钮去下载页手动安装（应用内无后台自动更新）
4. 不想更的版本可点"跳过此版"，下次不再打扰

### 数据管理

**功能**：管理应用数据和设置

**可用操作**：
1. **清空当前项目**：
   - 保留项目框架，清空所有内容
   - 从灵感阶段重新开始

2. **删除当前项目**：
   - 完全删除选中的项目
   - 不可恢复，请谨慎操作

3. **恢复出厂设置**：
   - 清除所有数据，包括 API 密钥
   - 应用将重启恢复初始状态

---

## 实用技巧与最佳实践

### 创作效率提升技巧

#### 1. 有效使用知识库
- **分类上传**：按类别整理参考资料
- **关键信息提取**：上传前提取文档核心内容
- **定期更新**：根据创作进展更新知识库

#### 2. 提示词优化方法
- **具体明确**：给出详细的背景和要求
- **分步指导**：复杂任务分解为多个步骤
- **示例引导**：提供期望的输出格式示例

#### 3. 批量生成技巧
- **章节批量规划**：在细纲页基于大纲自动生成所有章节概要
- **正文批量生成**：在写作生成弹窗里切批量模式，一次生成 5 或 10 章正文
- 一次只做一种批量，生成中可随时停止，已落定的章节不受影响

#### 4. 输出模式选择
- **流式输出**适合：
  - 需要实时调整生成方向
  - 生成长篇内容
  - 希望观察 AI 思考过程

- **传统输出**适合：
  - 需要快速获取结果
  - 生成简短内容
  - 网络环境不稳定时

### 创作流程优化

#### 1. 迭代式创作
1. 快速生成初版内容
2. 人工编辑和调整
3. 使用 AI 优化改进
4. 重复直到满意

#### 2. 质量检查清单
- [ ] 角色动机是否合理
- [ ] 情节逻辑是否连贯
- [ ] 节奏控制是否得当
- [ ] 文笔风格是否一致
- [ ] 细节描写是否充分

#### 3. 项目管理建议
- **定期备份**：每周导出项目数据
- **版本管理**：重要修改前创建备份
- **进度跟踪**：使用章节完成状态跟踪进度

---

## 故障排除

### 常见问题解决

#### 1. 应用无法启动
**可能原因**：
- 安装包损坏或不完整
- 系统版本过低
- 安全软件拦截

**解决方案**：
1. 重新下载安装包并安装
2. 确认系统为 Windows 10/11（或受支持的 macOS/Linux 版本）
3. 检查安全软件是否拦截，放行后再试

#### 2. AI 功能不可用
**可能原因**：
- API 密钥未配置或失效
- 网络连接问题
- 模型配置错误

**解决方案**：
1. 检查模型设置中的 API 密钥
2. 测试网络连接
3. 重新配置模型参数

#### 3. 数据保存失败
**可能原因**：
- 磁盘空间不足
- 文件权限问题
- 应用数据目录损坏

**解决方案**：
1. 检查磁盘可用空间
2. 以管理员身份运行应用
3. 清理临时文件
4. 手动备份后重置应用

#### 4. 性能问题
**可能原因**：
- 项目数据过大
- 同时打开过多功能
- 系统资源不足

**解决方案**：
1. 导出并清理旧项目
2. 关闭不必要的功能面板
3. 重启应用释放内存
4. 升级硬件配置

### 错误信息解读

#### "API 密钥无效"
- 检查密钥是否正确复制
- 确认密钥是否有使用权限
- 查看密钥是否过期

#### "网络连接失败"
- 检查网络连接状态
- 确认防火墙设置
- 设置 → 通用 → 网络代理填入代理地址（留空直连），点"测试连接"对照

#### "生成内容过长"
- 减少输入内容长度
- 调整最大令牌数设置
- 分多次生成

#### "存储空间不足"
- 清理磁盘空间
- 导出并删除旧项目
- 移动数据到其他磁盘

### 获取帮助

1. **查看日志**：
   - 开发工具控制台（F12）
   - 主进程终端输出
   - 应用数据目录中的日志文件

2. **社区支持**：
   - GitHub Issues 提交问题
   - 开发者文档参考
   - 用户交流群组

3. **联系开发团队**：
   - 通过官方渠道反馈问题
   - 提供详细的错误信息
   - 附上相关截图和日志

---

## 附录

### 快捷键参考

应用默认提供以下快捷键（均为 `Ctrl`，macOS 下对应 `Cmd`），设置 → 通用 → 快捷键可改键：

#### 分区导航（工作台内有效）
- `Ctrl+1`：切换到灵感分区
- `Ctrl+2`：切换到世界分区
- `Ctrl+3`：切换到角色分区
- `Ctrl+4`：切换到结构分区（大纲/细纲）
- `Ctrl+5`：切换到写作分区

#### 助手与编辑器
- `Ctrl+J`：显隐 AI 助手侧边栏
- `Ctrl+F`：写作编辑器查找条开关
- `Ctrl+Shift+X`：在提示词模板编辑中插入占位符行内原子（不可改键）

### 术语解释

#### AI 相关术语
- **Token**：AI 处理文本的基本单位，约等于 0.75 个英文单词
- **温度（Temperature）**：控制 AI 创造性的参数，值越高创造性越强
- **流式输出**：实时逐步显示生成内容
- **提示词（Prompt）**：给 AI 的指令和上下文

#### 创作术语
- **灵感碎片**：创作初期的零散想法
- **知识库**：创作参考的资料集合
- **虚拟章节**：序号为负或 id 在遗留清单的内部哨兵章节，不进章节列表，用于 AI 历史与草稿槽位，用户不可见也无需管理
- **角色关系图**：可视化展示角色关系的图表

### 资源链接

#### 官方资源
- 项目仓库：<https://github.com/chen647208/hongyue-creation>
- 最新版本下载：<https://github.com/chen647208/hongyue-creation/releases/latest>
- 在线文档：<https://chen647208.github.io/hongyue-creation/>

#### 学习资源
- 写作技巧与创作方法见文档站的「功能说明」与「设计蓝图」分区。
- 提问与反馈走 GitHub Issues：<https://github.com/chen647208/hongyue-creation/issues>

### 版本历史

版本号以 `package.json` 为准，变更记录见 `CHANGELOG.md`，此处不再另行维护。

---

## 结语

红月创作是一个强大的创作工具，它将 AI 的智能与人类的创造力相结合，帮助你更高效地完成小说创作。无论你是经验丰富的作家还是刚刚起步的新手，这个工具都能为你提供有价值的帮助。

记住，AI 是辅助工具，真正的创作灵魂来自于你。使用红月创作来激发灵感、提升效率。
